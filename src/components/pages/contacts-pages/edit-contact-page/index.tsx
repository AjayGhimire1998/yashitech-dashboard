import * as React from 'react';

interface IEditContactProps {
}

const EditContact: React.FunctionComponent<IEditContactProps> = (props) => {
  return <div> Editing a contact</div>
};

export default EditContact;
